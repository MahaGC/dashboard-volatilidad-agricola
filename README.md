#Dasboard de volatilidad Agrícola
Este prototipo analiza la evolución histórica y predice precios de commodities.

#Cómo ejecutar:
1. Instalar dependencias: `pip install -r requirements.txt`
2. Correr la app: `streamlit run app.py`
